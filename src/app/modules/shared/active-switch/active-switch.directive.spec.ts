import { ActiveSwitchDirective } from './active-switch.directive';

describe('ActiveSwitchDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveSwitchDirective();
    expect(directive).toBeTruthy();
  });
});
