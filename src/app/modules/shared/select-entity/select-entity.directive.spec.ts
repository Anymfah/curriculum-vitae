import {SelectEntityDirective} from './select-entity.directive';

describe('EntitySelectDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectEntityDirective();
    expect(directive).toBeTruthy();
  });
});
